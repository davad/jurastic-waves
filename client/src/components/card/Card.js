
import React from 'react';

import style from './style';

const Card = (props) => (
  <div className={style.card}>
    <div>DINO</div>
    <div className={style.toolbelt}></div>
  </div>
);

export default Card;
